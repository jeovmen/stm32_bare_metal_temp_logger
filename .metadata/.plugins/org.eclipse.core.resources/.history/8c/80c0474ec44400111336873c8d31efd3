
#include <cstdint>

using std::uint32_t;

constexpr uint32_t ADC1EN = (1U<<8);
constexpr uint32_t CR1_EOCIEN = (1<<5);


struct AdcConfig{

};

struct AdcRegisters{
};

class Adc
{
public:

    : 
    
private:

};
