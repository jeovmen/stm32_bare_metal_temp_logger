
#include <cstdint>

using std::uint32_t;

constexpr uint32_t GPIOAEN = (1U<<0);
constexpr uint32_t ADC1EN = (1U<<8);
constexpr uint32_t CR1_EOCIEN = (1<<5);


struct AdcConfig{
	uint32_t ADCEN;

};

class Adc
{
public:

    : 
    
private:

};
