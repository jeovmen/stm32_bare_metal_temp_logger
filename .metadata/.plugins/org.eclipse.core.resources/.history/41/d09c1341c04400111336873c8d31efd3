
#include <cstdint>

using std::uint32_t;

constexpr uint32_t GPIOAEN = (1U<<0);
constexpr uint32_t ADC1EN = (1U<<8);


struct AdcConfig{

};

class Adc
{
public:
    explicit Adc(const AdcConifig &cfg)
    : 
    
private:

};